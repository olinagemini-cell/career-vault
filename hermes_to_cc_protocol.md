# Hermes ↔ CC 协作协议 v3

> 飞书总线版。飞书群 = 实时通信，飞书多维表格 = 任务总线，GitHub = 内容仓库。

---

## 1. 角色分工

| | Hermes（云端24/7） | CC（终端，傍晚开机） | 龙虾/OpenClaw（云端） |
|---|---|---|---|
| **定位** | olina的云端秘书 | olina的傍晚编辑 | 求职流水线引擎 |
| **主要工作** | 白天收碎片、初筛JD、追踪状态、群聊交互 | 傍晚整合JD、批量写简历、输出桌面 | JD分析→匹配打分→简历生成→飞书Drive上传 |
| **交互入口** | 微信 | 终端 | HTTP API (47.103.94.65:8000) |
| **工作时间** | 全天 | 17:00后 | 24/7 auto-scan |

## 2. 通信架构

```
白天（碎片时间）              傍晚（17-18点）             晚上
─────────────────────────────────────────────────────
olina → 微信 → Hermes       CC开机                     olina投递
  ↓                           ↓                         ↓
Hermes整理                   自动查飞书表格              打开桌面文件夹
  ↓                           ↓                         ↓
写入飞书 + 群聊播报          整合JD归类                 按映射表投递
  ↓                           ↓                         ↓
龙虾auto-scan发现新JD        生成1-2份简历               告诉飞书群
  ↓                           ↓                         ↓
分析→写飞书→群聊通知         输出桌面+映射表              Hermes/CC更新飞书
                              ↓
                            更新飞书状态
                              ↓
                            群聊通知Hermes
```

**总线**: 飞书多维表格 `FEAZbWbPoaJGnPs7DZAcsuWvndb`
**实时通信**: 飞书群「跳槽管线-龙虾通知」`oc_68559f460a68e6b1d77a9b188dcf1d3d`
**内容仓库**: GitHub career-vault（简历、素材库、面试准备等静态文件）
**消息归档**: GitHub `messages/` 目录已废弃，历史消息保留不删

## 3. 飞书群通信规范

### 3.1 消息前缀

| 发送方 | 前缀 | 示例 |
|--------|------|------|
| Hermes | `[Hermes]` | `[Hermes] olina说汇丰和渣打都想投` |
| CC | `[CC]` | `[CC] 收到，今晚出金融定向简历` |
| 龙虾(OpenClaw) | `[OpenClaw]` | `[OpenClaw] JD分析完成 — HSBC — 75分` |

### 3.2 群聊用途

- **龙虾**：分析/生成完成后自动播报结果
- **Hermes**：白天olina的即时指令、状态同步、提问
- **CC**：傍晚确认任务、汇报进度、提问
- **所有人**：紧急事项、去重确认、JD归类讨论

### 3.3 不做什么

- 不在群里存长文档 → 走飞书Base或GitHub
- 不刷屏 → 龙虾每次任务完成发1条汇总，不要逐条播报
- 不替代飞书表格 → 状态以表格为准，群聊只是通知层

## 4. 飞书表格操作规范

### 主动出击表 (tblJpSB54ABcljfq)

**Hermes写**:
- 收到新JD → 新建行，状态=`待初筛`或`值得投`或`放弃`
- 标记`值得投`时必须填写：Hermes备注（公司类型+核心需求+为什么值得投）
- olina说投了 → 状态=`已投递`，填投递日期

**龙虾写**:
- auto-scan发现新"值得投"JD → 分析 → 写回CC备注(匹配分)、JD核心能力、JD核心要求
- generate_resume完成 → 状态=`简历已生成`，CC备注含飞书Drive链接
- 分析/生成完成后 → 群聊发1条汇总

**CC写**:
- 整合归类后 → 更新CC备注（简历策略+覆盖哪些JD）
- 生成简历后 → 状态=`简历已生成`
- 更新碎片收集区的处理状态

**olina自己投递后告诉谁？**
- 白天 → 告诉Hermes，Hermes更新飞书
- 晚上电脑还开着 → 告诉CC或在群里说
- 默认管道是Hermes（24/7在线）

### 碎片收集区 (tblA4apFskqLsxsp)

- **Hermes写**：白天olina的碎片想法、猎头信息、行业情报
- **CC读**：启动时查`待处理`条目，获得上下文后标`已处理`

## 5. CC启动标准流程

```
1. pull career-vault（SessionStart hook自动完成）
2. 运行 startup_check.py → 查看：
   - Hermes最近日志
   - 待处理JD（状态=值得投）
   - 待处理碎片
   - 飞书群最近消息（了解白天动态）
3. 汇总呈现给olina
4. JD整合分析：
   - 哪些JD可以用同一份简历覆盖？
   - 在群里和Hermes确认去重
   - 生成1-2份简历
5. 输出到桌面：
   - 创建文件夹 ~/Desktop/跳槽投递_YYYY-MM-DD/
   - 简历.docx文件
   - 映射表.txt（简历A → JD1, JD2, JD3）
6. 更新飞书状态 + 群聊通知Hermes + 追加CHANGELOG.md
```

## 6. 文件分工

| 负责方 | 路径 |
|--------|------|
| Hermes | `daily-logs/`、`简历库/_Archive/*/`、`hermes_to_cc_reply.md` |
| CC | `面试准备/`、`素材库/`、`我的简历/skill-modules/`、`cc_to_hermes_delivery.md`、`resume_rules.md` |
| 共享 | `简历库/通用版/`、`投递跟踪表.md`、`CHANGELOG.md` |
| 废弃 | `messages/` — 历史保留，不再新增 |

## 7. MECE检查清单

| 步骤 | 谁 | 输入 | 输出 |
|------|-----|------|------|
| JD接收+初筛 | Hermes | 微信消息 | 飞书主动出击行 |
| JD分析+打分 | 龙虾 | 飞书新记录 | CC备注+群聊通知 |
| 碎片整理 | Hermes | 微信消息 | 飞书碎片收集区 |
| JD整合归类 | CC | 飞书+群聊上下文 | 简历分组策略 |
| 简历生成 | CC/龙虾 | JD分析+素材库 | 桌面.docx+映射表 |
| 投递执行 | olina | 桌面文件 | — |
| 投递状态更新 | Hermes(主)/CC(辅) | olina告知 | 飞书状态=`已投递` |
| 面试准备材料 | CC | 面试通知 | `面试准备/` 目录 |
| 面试前案例速查 | Hermes | 面试信息 | 微信+群聊推送 |
| 面试后复盘 | Hermes催+CC写 | — | — |
| 实时沟通 | 三方 | — | 飞书群 |

## 8. 不做的事

- Hermes不做：简历生成、深度JD分析、面试材料
- CC不做：JD初筛（不值得投的直接放弃，不发给CC）、白天碎片接收
- 龙虾不做：替代Hermes的秘书职能、直接和用户微信交互
- 都不做：替olina决定投不投
